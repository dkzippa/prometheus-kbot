# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
replicaCount: 1
image:
  registry: "ghcr.io"
  repository: "dkzippa/prometheus-kbot"
  tag: "v1.0.10-9934b3a"
  os: linux
  arch: amd64
secret:
  env: "TELE_TOKEN"
  name: "kbot"
  key: "token"
